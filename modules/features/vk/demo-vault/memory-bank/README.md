# Memory bank

Durable, agent-relevant context for this vault - recurring themes,
in-flight projects, conventions specific here. Plain Markdown, no
frontmatter required. Never overwritten by `vk`; grow it freely.

This is the `vk` demo/fixture vault - its own "durable context" is
simply: keep this vault exercising every `vk` feature as the tool
changes (see this vault's own `AGENTS.md` and `README.md`).
