# Current Objective

_What is this project trying to accomplish right now, in a sentence or
two? Replace this line once real work starts._

# Active Theorem

_Which theorem (see `theorem-roadmap.md`) is currently being worked, by
which role, and what's the immediate sub-goal?_

# Current Blockers

_Anything stopping forward progress: a missing lemma, an unresolved
question in `docs/architecture/open-questions.md`, a proof that won't
close. One bullet per blocker, newest first._

# Recent Discoveries

_Surprising interactions between definitions, lemmas that turned out to
be needed, dead ends already explored (so nobody re-explores them).
Newest first; prune entries once they're absorbed into
`semantic-domains.md`/`theorem-roadmap.md` and no longer "recent"._

# Next Session Start Here

_The single most useful pointer for whoever (human or agent) picks this
up next - a file, a theorem identifier, or a concrete next step. Keep
this short; it's the first thing read at the next session's startup._
